# Shared protocol artifacts

Machine-readable protocol schemas and golden fixtures will live here when protocol v1 is specified in issue #2. This directory is intentionally documentation-only in the buildable baseline; `docs/protocol.md` records the currently implemented HTTP observability contract.
